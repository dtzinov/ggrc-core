behave
Flask-Testing
-e git+https://github.com/rbarrois/factory_boy.git#egg=factory-boy
mock
requests
PyYAML
# HamlPy is required here to pull in "requirements" which aren't
# actually required but will cause `import hamlpy` to fail
# Requirements are: django, Jinja2, markdown, pygments
HamlPy
